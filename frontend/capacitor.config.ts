import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.wildhummingbirdplay',
  appName: 'wild-hummingbird-play',
  webDir: 'dist'
};

export default config;
